{
  "name": "natter",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "pretest": "node -e \"const fs=require('fs');const want=parseInt(fs.readFileSync('.nvmrc','utf8'),10);const got=parseInt(process.versions.node,10);if(got<want){console.error('\\nNatter needs Node >='+want+' (.nvmrc) — you are on '+process.version+'. Run: nvm use\\n');process.exit(1)}\"",
    "test": "node --experimental-vm-modules --test tests/"
  },
  "dependencies": {
    "next": "16.2.9",
    "openai": "^6.42.0",
    "pg": "^8.21.0",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "redis": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^9",
    "eslint-config-next": "16.2.9"
  }
}
